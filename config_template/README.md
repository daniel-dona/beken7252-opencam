# Camera configuration using an SD card

1. Format a compatible SD card, max capacity supported is 32GB (SDHC)*. Must be FAT formatted.
2. [TODO]






















* I'm not totally sure about this, technically the difference is that we must support exFAT to handle SDXC (>32GB) cards. But is not a hardware limitation. To explore in the future as FatFS is already in the SDK.